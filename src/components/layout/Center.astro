---
// Thin wrapper around the `.cs-center` CUBE layout primitive
// (src/assets/css/cube/layout.css) so pages don't repeat the class name.
interface Props {
  class?: string
  [key: string]: unknown
}

const { class: className, ...rest } = Astro.props
---

<div class:list={['cs-center', className]} {...rest}>
  <slot />
</div>
